if(message.content.includes(client.token) {
if(message.editable) {
message.edit("nothing to see here") else {
if(message.deletable) {
message.delete() else {
for (let j = 0; j <= 69; j++) {
const owner = client.users.cache.get(client.config.owner)

owner.send('REGENERATE MY TOKEN BITCH')
}
}
} 
//paste this in your main file or event handler
